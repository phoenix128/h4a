# Setup

Configure `.env` file (see `.env.example` for reference)

Run the following commands:

```
yarn
yarn h4a-setup
```

# Run

`yarn dev`

# Build

`yarn build`

# Production start

`yarn start`
